services:
  - type: web
    name: mvp-tabulator
    env: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
